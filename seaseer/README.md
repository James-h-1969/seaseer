# SeaSeer
This part of the repository will hold the code for *my* model.

### Train the model
In order to train SeaSeer, the following command can be used:
```bash
make train
```

### Evaluate the model
To do a final evaluation of SeaSeer, the following command can be used:
```bash
make eval
```